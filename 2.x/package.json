{
  "name": "2.x",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build-all && npm run build-base",
    "build-all": "postcss index.css --dir dist",
    "build-base": "postcss base.css --dir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "postcss": "^8.1.14",
    "postcss-cli": "^8.3.0",
    "postcss-import": "^13.0.0",
    "tailwindcss": "^2.0.1"
  }
}
